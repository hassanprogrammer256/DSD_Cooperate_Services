# Copy to .env for local dev. Never commit the real .env file.

DJANGO_SECRET_KEY=generate-with-get_random_secret_key
TAP_SECRET_KEY=
PUBLIC_SITE_URL=http://localhost:5173
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=info@dsdgrp.com
LEAD_NOTIFICATION_EMAIL=info@dsdgrp.com
# Inert until a real external CRM exists — leave empty. See leads/webhooks.py.
LEAD_WEBHOOK_URL=

# Production only (config/settings/prod.py) — not read in dev:
# DJANGO_ALLOWED_HOSTS=api.dsdcop.com
# DATABASE_URL=postgres://user:password@host:5432/dbname
# CORS_ALLOWED_ORIGINS=https://dsdcop.com,https://admin.dsdcop.com
# HTTPS_ENABLED=True — leave unset (defaults True) once a real TLS cert is in front of
# this deploy; set to False only for a bare-IP/no-domain deploy with no HTTPS listener.
